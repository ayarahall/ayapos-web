# Backend API URL (no trailing slash)
# In development this is handled by vite.config.ts proxy — leave empty
# In production set this to your deployed backend URL e.g. https://api.ayapos.com
VITE_API_BASE_URL=
